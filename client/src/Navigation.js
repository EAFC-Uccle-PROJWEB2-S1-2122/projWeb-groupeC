const Navigation = () => {
  return (
    <div className="navigation">
      <div>
        <button className="active">Rechercher un livre</button>
        <button>Encoder un livre</button>
      </div>
    </div>
  );
};

export default Navigation;
